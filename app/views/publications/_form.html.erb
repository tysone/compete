<%= form_for @publication, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :organization_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :organization_name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :publication_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :publication_name, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                publications_path, :class => 'btn' %>
  </div>
<% end %>
